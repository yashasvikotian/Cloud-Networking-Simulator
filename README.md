# Cloud-Networking-Simulator
It manages customizable network profiles (latency, bandwidth, reliability) to simulate real-world conditions, supports create/update/delete + persist via file storage, runs connectivity tests &amp; performance checks, and even uses ADB commands (like airplane mode toggle and profile switching) to control device network settings.
